# homebrew-simpalarm

Homebrew tap for SimpAlarm.

## Install

```bash
brew tap dctmfoo/simpalarm
brew install --cask simpalarm
```

The `simpalarm` cask is published from the signed and notarized GitHub releases in `dctmfoo/simpalarm`.
